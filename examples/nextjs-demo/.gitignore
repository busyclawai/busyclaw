# Next's build output and dev cache. Regenerated by `next dev` / `next build`, and large enough
# that committing it once buries every real diff afterwards.
.next/
out/

# Next rewrites this on every dev/build; kept out so it never shows up as a spurious change.
next-env.d.ts
